apiVersion: v1
kind: PersistentVolume

metadata:
  name: weather-pv

spec:
  capacity:
    storage: 1Gi

  accessModes:
    - ReadWriteOnce

  persistentVolumeReclaimPolicy: Retain

  storageClassName: manual

  hostPath:
    path: /run/desktop/mnt/host/c/WEATHERMLOPS/model_registry